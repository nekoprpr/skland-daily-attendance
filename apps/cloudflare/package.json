{
  "name": "@skland-x/cloudflare",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev --test-scheduled",
    "start": "wrangler dev --test-scheduled",
    "build": "wrangler build",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "test": "vitest",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@date-fns/tz": "catalog:",
    "date-fns": "catalog:",
    "defu": "catalog:",
    "skland-kit": "catalog:lib",
    "statocysts": "catalog:lib",
    "unctx": "catalog:",
    "unstorage": "catalog:"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "catalog:",
    "wrangler": "catalog:"
  }
}
